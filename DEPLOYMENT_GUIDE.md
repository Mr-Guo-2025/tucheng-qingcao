# GitHub Pages 部署指南

本指南將教您如何將 Vue 專案部署到 GitHub Pages。

## 📋 前置條件

- ✅ GitHub 帳號
- ✅ Git 已安裝
- ✅ 專案已經配置好（vite.config.js 中的 base 路徑已設定）

## 🚀 方法一：使用 GitHub Actions 自動部署（推薦）

這是最簡單的方法，每次推送代碼時會自動構建和部署。

### 步驟 1：推送代碼到 GitHub

```bash
# 如果還沒有初始化 Git，先初始化
git init

# 添加所有檔案
git add .

# 提交變更
git commit -m "Initial commit with GitHub Actions deployment"

# 添加遠端倉庫（如果還沒添加）
git remote add origin https://github.com/Mr-Guo-2025/tucheng-qingcao.git

# 推送到 GitHub
git push -u origin main
```

### 步驟 2：在 GitHub 上啟用 GitHub Pages

1. 前往您的 GitHub 倉庫：https://github.com/Mr-Guo-2025/tucheng-qingcao
2. 點擊 **Settings**（設定）
3. 在左側選單中點擊 **Pages**
4. 在 **Source** 下拉選單中選擇 **GitHub Actions**
5. 儲存設定

### 步驟 3：等待部署完成

1. 前往 **Actions** 標籤頁
2. 您會看到工作流程正在執行
3. 等待綠色勾勾出現（通常需要 1-2 分鐘）
4. 部署完成後，您的網站將會在以下網址上線：
   ```
   https://mr-guo-2025.github.io/tucheng-qingcao/
   ```

### 🎉 完成！

之後每次您推送代碼到 `main` 分支時，網站都會自動重新構建和部署！

---

## 🔧 方法二：手動部署到 gh-pages 分支

如果您想要手動控制部署過程，可以使用這個方法。

### 步驟 1：安裝 gh-pages 套件

```bash
npm install --save-dev gh-pages
```

### 步驟 2：在 package.json 中添加部署腳本

在 `scripts` 區塊中添加：

```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "deploy": "npm run build && gh-pages -d dist"
}
```

### 步驟 3：執行部署

```bash
npm run deploy
```

這個指令會：

1. 構建您的專案
2. 將 `dist` 目錄的內容推送到 `gh-pages` 分支
3. 自動部署到 GitHub Pages

### 步驟 4：在 GitHub 上設定 Pages

1. 前往您的 GitHub 倉庫設定
2. 點擊 **Pages**
3. 在 **Source** 中選擇 **Deploy from a branch**
4. 選擇 **gh-pages** 分支和 **/ (root)** 目錄
5. 點擊 **Save**

---

## 🔍 驗證部署

部署完成後，您可以：

1. **檢查 Actions**（方法一）：

   - 前往 Actions 標籤頁
   - 查看最新的工作流程執行狀態

2. **檢查 Pages 設定**：

   - 前往 Settings > Pages
   - 您會看到網站的 URL

3. **訪問網站**：
   - 開啟瀏覽器
   - 前往：https://mr-guo-2025.github.io/tucheng-qingcao/

---

## 🐛 常見問題

### 問題 1：頁面顯示 404

**解決方案**：

- 確認 `vite.config.js` 中的 `base` 設定正確：
  ```javascript
  base: "/tucheng-qingcao/";
  ```
- 確認倉庫名稱與 base 路徑一致

### 問題 2：CSS/JS 檔案載入失敗

**解決方案**：

- 檢查瀏覽器控制台的錯誤訊息
- 確認所有資源路徑都是相對路徑
- 重新構建並部署

### 問題 3：路由在重新整理時顯示 404

**解決方案**：

- GitHub Pages 不支援 SPA 的 history 模式
- 考慮使用 hash 模式，或添加 404.html 重定向

---

## 📝 更新網站

### 使用 GitHub Actions（方法一）

```bash
# 修改代碼後
git add .
git commit -m "Update website"
git push
# GitHub Actions 會自動部署
```

### 使用手動部署（方法二）

```bash
# 修改代碼後
npm run deploy
```

---

## 🎯 建議的工作流程

1. **開發階段**：使用 `npm run dev` 在本地開發
2. **測試階段**：使用 `npm run build` 和 `npm run preview` 測試打包結果
3. **部署階段**：推送到 GitHub（方法一）或執行 `npm run deploy`（方法二）

---

## 📚 相關資源

- [GitHub Pages 官方文檔](https://docs.github.com/en/pages)
- [GitHub Actions 文檔](https://docs.github.com/en/actions)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)

---

**祝您部署順利！** 🚀
